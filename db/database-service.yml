apiVersion: v1
kind: Service
metadata:
  name: database-service
  labels:
    app: database
spec:
  ports:
  - port: 27017
    name: database-statefulset
  clusterIP: None
  selector:
    app: database
